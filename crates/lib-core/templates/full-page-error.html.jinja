{% extends "base.html.jinja" %}
{% block body %}
  <main class="flex flex-row gap-5 items-center justify-center h-screen">
    <h1 class="text-3xl">{{ code }}</h1>
    <span class="text-xl">{{ message }}</span>
  </main>
{% endblock body %}
